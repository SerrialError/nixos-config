--- a/configuration.nix
+++ b/configuration.nix
@@ -40,6 +40,7 @@
   nixpkgs.config.allowUnfree = true;
   nixpkgs.config.permittedInsecurePackages = [
 	"electron-36.9.5"
+	"electron-39.8.10"
   ];
   nixpkgs.overlays = [
     (final: prev: {
